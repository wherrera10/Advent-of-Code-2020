
function yellowstone(N)
    ylist = [1, 2, 3]
    b = Dict(1 => 1, 2 => 1, 3 => 1)
    while length(ylist) < N
        i = 4
        while true
            if !haskey(b, i) && (gcd(i, ylist[end]) == 1) && (gcd(i, ylist[end - 1]) > 1)
                push!(ylist, i)
                b[i] = 1
                break
            else
                i += 1
            end
        end
    end
    return ylist
end

println(yellowstone(10))


