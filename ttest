s = "17.89    101⅔  34½  22"
d = Dict('½' => 1/2, '⅔' => 2/3)
allfrac = join(keys(d), "|")
str = replace(s, Regex("\\d*(" * allfrac * ")") => (x) -> string(parse(Float64, x[1:end-1]) + d[x[end]]))
println(str)

