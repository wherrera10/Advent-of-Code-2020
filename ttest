win = Window("Test")
label = Label("Some text")
a = GtkFrame("test")
push!(win, a)
push!(a, label)
sc = Gtk.GAccessor.style_context(lbl)
pr = CssProviderLeaf(data="label {font-size:32px;}")
push!(sc, StyleProvider(pr), 600)
showall(win)
