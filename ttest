using Crayons
 
while(occursin(r"^[\d\w]{32}", (s = readline()))
    (crc, restofit) = split(s, s"\s+", limit=2)
    for i in 1:3:length(crc)
        hexcolor = crc[i] * "0" * crc[i + 1] * "0" * ctc[i + 2] * "0"
        print(@crayon_str(hexcolor), crc[i:i + 2])
    end
    println(restofit)
end


