
function okabbrev(dict, abb, ref)
    if length(abb) <= length(ref) && abb == ref[1:length(abb)]
        for i in length(abb):-1:1
            if haskey(dict, abb[1:i])
                return dict[abb[1:i]]
            end
        end
    end
    return "*error*"
end










		
